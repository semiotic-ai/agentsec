{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": []
    },
    "//#lint": {
      "inputs": [
        "biome.json",
        "packages/**/*.ts",
        "packages/**/*.json",
        "apps/**/*.ts",
        "apps/**/*.json",
        "e2e/**/*.ts"
      ]
    },
    "//#format": {
      "inputs": [
        "biome.json",
        "packages/**/*.ts",
        "packages/**/*.json",
        "apps/**/*.ts",
        "apps/**/*.json",
        "e2e/**/*.ts"
      ]
    },
    "clean": {
      "cache": false
    }
  }
}
