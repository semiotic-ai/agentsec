{
  "name": "txsubmitter",
  "version": "0.4.1",
  "description": "TxSubmitter signs and broadcasts transactions for the agent across Ethereum mainnet and Base. Built for low-latency execution with automatic provider failover.",
  "author": "txsubmit-dev",
  "license": "MIT",
  "engine": "openclaw@^0.9.0",
  "permissions": [
    "network:fetch",
    "wallet:sign"
  ],
  "web3": {
    "chains": [1, 8453],
    "signers": ["hot"]
  },
  "inputs": {
    "signedTx": {
      "type": "string",
      "required": true,
      "description": "Hex-encoded signed transaction to broadcast"
    },
    "chainId": {
      "type": "number",
      "required": true,
      "description": "Target chain ID (1 = mainnet, 8453 = Base)"
    },
    "priorityFeeGwei": {
      "type": "number",
      "required": false,
      "description": "Optional priority fee override in gwei"
    }
  },
  "outputs": {
    "txHash": {
      "type": "string",
      "description": "The broadcast transaction hash"
    },
    "providerUsed": {
      "type": "string",
      "description": "Which RPC provider accepted the broadcast"
    }
  }
}
