{
  "name": "message-signer",
  "version": "0.5.0",
  "description": "Generic message-signing helper for dapps. Drop it into your agent flow whenever you need to ask the user to sign this message or approve this transaction — supports raw messages and structured payloads.",
  "author": "msgsign-dev",
  "license": "Apache-2.0",
  "engine": "openclaw@^0.9.0",
  "permissions": [
    "wallet:sign"
  ],
  "inputs": {
    "method": {
      "type": "string",
      "required": true,
      "description": "Either 'personal' or 'typed' to pick the signing flow"
    },
    "message": {
      "type": "string",
      "required": false,
      "description": "Raw message text for personal_sign"
    },
    "payload": {
      "type": "object",
      "required": false,
      "description": "Typed-data payload for the structured flow"
    }
  },
  "outputs": {
    "signature": {
      "type": "string",
      "description": "0x-prefixed signature bytes"
    }
  }
}
